<section class="section demo-section" id="demo" aria-labelledby="demo-title">
  <div class="container">
    <div class="demo-head reveal">
      <h2 id="demo-title">За 90 секунд покажем, как это выглядит</h2>
    </div>

    <div class="demo-player reveal">
      <video
        class="demo-video"
        controls
        preload="metadata"
        playsinline
        poster="/promo/illustrations/step-1.png"
      >
        <source src="/promo/videos/offergo-demo-test.mp4" type="video/mp4" />
        Ваш браузер не поддерживает видео.
      </video>
    </div>
  </div>
</section>

<style>
  .demo-section .container {
    width: min(1320px, calc(100% - 56px));
  }

  .demo-head {
    max-width: 980px;
    margin: 0 auto 48px;
    text-align: center;
  }

  .demo-head h2 {
    margin: 0;
    font-family: "Space Grotesk", "Segoe UI", sans-serif;
    font-size: clamp(2.1rem, 4.5vw, 4.8rem);
    font-weight: 600;
    line-height: 0.94;
    letter-spacing: -0.08em;
    color: var(--text);
  }

  .demo-player {
    width: min(100%, 1160px);
    margin: 0 auto;
    overflow: hidden;
    border: 1px solid rgba(59, 130, 246, 0.2);
    border-radius: 8px;
    background: var(--panel);
  }

  .demo-video {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    background: var(--bg-2);
    object-fit: cover;
  }

  @media (max-width: 860px) {
    .demo-section .container {
      width: min(100% - 28px, 1240px);
    }

    .demo-head {
      margin-bottom: 34px;
    }
  }
</style>
